The TypeScript errors you're encountering indicate that you're trying to pass a `data` prop to `PayloadTable` and `WordlistTable` components, but these components don't accept any props (they're defined without any parameters).

Here's how to fix the issues:

1. First, update your `PayloadTable` component to accept a `data` prop:

```tsx
// components/database/payload-table.tsx
interface PayloadTableProps {
  data: {
    id: string;
    name: string;
    type: string;
    size: string;
    filePath?: string;
    sourceUrl?: string;
    previewAvailable: boolean;
  }[];
}

export const PayloadTable = ({ data }: PayloadTableProps) => {
  // ... rest of the component code remains the same
  // Just replace all instances of `payloadTemplates` with `data`
}
```

2. Similarly, update your `WordlistTable` component:

```tsx
// components/database/wordlist-table.tsx
interface WordlistTableProps {
  data: {
    id: string;
    name: string;
    type: string;
    size?: string;
    filePath?: string;
    sourceUrl?: string;
    previewAvailable: boolean;
  }[];
}

export const WordlistTable = ({ data }: WordlistTableProps) => {
  // ... rest of the component code remains the same
  // Just replace all instances of `wordlists` with `data`
}
```

3. Alternatively, if you prefer to keep using the imported `payloadTemplates` and `wordlists` directly in the components (as your current implementation does), then you should remove the `data` prop being passed in your page component:

```tsx
// In app/database/page.tsx
// Change this:
<PayloadTable data={payloadTemplates} />

// To this:
<PayloadTable />
```

And similarly for the WordlistTable:

```tsx
// Change this:
<WordlistTable data={wordlists} />

// To this:
<WordlistTable />
```

Choose one of these approaches based on your needs:
- If you want the tables to be reusable with different data sources, use the first approach (adding props)
- If these tables will always use the data from `security-resources.ts`, use the second approach (remove the prop passing)

Since your current table components are already using the imported data directly, the simpler solution would be to remove the prop passing from your page component.  